<div class="media">
  <div class="media-body">
    <h4 class="media-heading">
      <%= image_tag post.user.avatar_url(32) %>
      <%= link_to post.title, topic_post_path(post.topic, post) %>
    </h4>
    <small>
      <%= pluralize(post.comments.count, 'Comment') %>,  <%= pluralize(post.votes.count, 'Vote') %>
    </small>
  </div>
</div>
